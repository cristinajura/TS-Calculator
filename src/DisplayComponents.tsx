import React from 'react';

export const OutputDisplay = (props: any) => {
  return <div id="outputDisplay">{props.outputDisplay}</div>;
};

export const InputDisplay = (props: any) => {
  return <div id="display">{props.inputDisplay}</div>;
};

